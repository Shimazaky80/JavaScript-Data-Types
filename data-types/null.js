// The special null value forms a separate type of its 
// own which contains only the null value:
let age = null
console.log(age)

// is not a “reference to a non-existing object” or a 
// “null pointer” like in some other languages.
// It’s just a special value which represents “nothing”, 
// “empty” or “value unknown”.