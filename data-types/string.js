// A String is a series of characters surrounded by quotes
let str = "Hello"
console.log(str)
let str2 = 'Single quotes are ok too'
console.log(str2)
let phrase = `can embed another ${str}`
console.log(phrase)